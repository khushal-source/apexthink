[build]
  # Tell Netlify to enter the frontend directory before building
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# This is CRITICAL for React Router. It prevents 404 errors when refreshing the page.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
